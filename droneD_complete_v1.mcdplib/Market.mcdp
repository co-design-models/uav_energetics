mcdp {
  customer = instance `Customer
  engineering = instance `DroneComplete

  budget provided by customer >= total_cost_ownership required by engineering

  travel_distance provided by engineering >= travel_distance required by customer
  num_missions provided by engineering >= num_missions required by customer
  carry_payload provided by engineering >= carry_payload required by customer
}
